# Objective: Create one shared environment for all scientific-discovery tools.
name: scidiscovery
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
